steps:
  - name: 'hashicorp/terraform:1.6.3'
    args: ['init']
    env:
    - "TF_VAR_project-name=corded-shift-399205"
  - name: 'hashicorp/terraform:1.6.3'
    args: ['plan', '-out=tfplan']
    env:
    - "TF_VAR_project-name=corded-shift-399205"
  - name: 'hashicorp/terraform:1.6.3'
    args: ['destroy', 'tfplan' ]
    env:
    - "TF_STATE_OUT=cloudbuild.tfstate"
      #substitutions:
      #_BUCKET: terraformbuckettest
tags: ['cloud-builders-community']

